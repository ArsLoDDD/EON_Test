import { useDispatch } from 'react-redux'
import { userAuth } from '../redux/slices/authSlice'
import { UserLoginData } from '../types/userTypes'

const getUserAuth = async (obj: UserLoginData) => {}

export default getUserAuth
